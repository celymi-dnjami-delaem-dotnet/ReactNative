import { FullTheme } from 'react-native-elements';

const theme: Partial<FullTheme> = {
    colors: {
        primary: '#d73374',
        white: '#FFF',
        black: '#000',
    },
};

export default theme;
